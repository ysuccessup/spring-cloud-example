server:
  port: 8766

spring:
  application:
    name: service-hystrix-dashboard

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#开启断路器(feign自带)
feign.hystrix.enabled: true